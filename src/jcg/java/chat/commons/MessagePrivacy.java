package jcg.java.chat.commons;

public enum MessagePrivacy {

	PUBLIC, // Everyone can see the message
	PRIVATE, // Only specified person can see the message
	GROUP, // Only specified group can see the message
	 
	
}
